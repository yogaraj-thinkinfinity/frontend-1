      let age = 20;
      age < 18
      age > 18
      age <= 18
      age >= 18
      age == 18   // checks if both values are same
      age != 18
      age === 18  // checks if both values and datatypes are same
      age !== 18

Boolean("hi")   // true
Boolean("") // false
Boolean(10) // true
Boolean(-10)    // true
Boolean(0)  // false

 age >=10 && age<=25 // returns true only if all the conditions are satisfied. 
 age >=10 || age<=25 //  retuns true altlest if it satisfies any one of the conditions